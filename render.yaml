services:
  - type: web
    name: career-assistant-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
